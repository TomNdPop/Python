mark = 'y'
while mark == 'y':
    num1 = float(input('Enter a number: '))
    num2 = float(input('Enter a number: '))
    sum = num1+num2
    print(sum)

    #Asking for the continuation
    mark = input('Would you like to continue with the algorithm? (y/n)')
